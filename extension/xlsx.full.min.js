// Placeholder bundle for Excel export; this file will be replaced by a real bundle in a later step if needed.
(function(root, factory){
  var XLSX = factory();
  if (typeof module !== 'undefined') module.exports = XLSX;
  root.XLSX = XLSX;
})(typeof self !== 'undefined' ? self : this, function(){
  return {
    utils: {
      book_new: function(){ return {}; },
      aoa_to_sheet: function(data){ return { data: data }; },
      book_append_sheet: function(book, sheet, name){ book[name] = sheet; }
    },
    write: function(workbook, opts){
      return JSON.stringify(workbook);
    }
  };
});
